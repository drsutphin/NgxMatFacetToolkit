# Documentation

This folder holds the source docs for the repo (roadmap, release checklist, release notes, and session notes).
The `docs/` directory is reserved for the generated demo site output used by GitHub Pages; regenerate it with `npm run build-demo`.
